name = "eccleshallcfrs-app"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# KV namespace IDs are configured per-project in the Cloudflare Pages dashboard.
# Do not hardcode IDs here — it overrides dashboard bindings on every deploy,
# which breaks multi-group deployments sharing this codebase.
